		// var COL_DETECTOR = null;

		// function setPosition(x, y) {
		// 	var collision = cManager.adjust(x, y, spriteWidth, spriteHeight);

		// 	if (collision) {

		// 		if (instance.y + spriteHeight <= collision[1]) {
		// 			y = collision[1] - spriteHeight;
		// 		} else if (instance.y >= collision[1] + collision[3]) {
		// 			y = collision[1] + collision[3];
		// 		}

		// 		if (instance.x + spriteWidth <= collision[0]) {
		// 			x = collision[0] - spriteWidth;
		// 		} else if (instance.x >= collision[0] + collision[2]) {
		// 			x = collision[0] + collision[2];
		// 		}
		// 	}

		// 	var isMoved = (instance.x !== x || instance.y !== y);

		// 	instance.x = x;
		// 	instance.y = y;


		// 	return isMoved;
		// }

